# A service account is created with the rbac rules defined by clusterole and role resources. These rbac rules are same
# as those used by the windows-machine-config-operator. The reason being, we would like to test wni in an environment
# which matches the operator.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: windows-node-installer
  namespace: windows-node-installer
